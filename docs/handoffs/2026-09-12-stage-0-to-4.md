# Stage 0-4 research handoff

Date: 2026-09-12. Branch: `codex/stage-0-to-4`.

## Outcome

The authorized assessment and real aggregate pilot are complete. Read
[the consolidated report](../findings/stage_0_to_4_report.md). Recommendation C:
interesting empirical observation, weak paper. Original quote-time H1/H2 and
flexibility premium remain not estimable from DB1B; this is the authorized
partial-pilot interpretation, not a claim those tests ran.

Aggregate risk/interaction signs oppose expectations. Twelve route clusters,
endogenous traffic, quarterly timing, and sparse joint support prevent causal
claims. Preserve these negative findings. No further API acquisition is scheduled.

## Commits

- `bcb1b47`: Stage 0 original/aggregate specification.
- `9e594d1`: verified literature and novelty.
- `0641787`: Stage 2 data feasibility.
- `6e48581`: tested public-data pipeline.
- `6465419`: real pilot, provenance, and audits.
- `65420ec`: Stage 4 analysis, final reports, reproducibility checks.
- This handoff and publication status follow in the final documentation commit.

## Data and verification

Sixteen Market/Ticket ZIPs cover 2023-2024. Seven ERA5 responses, an airport registry,
and a small BTS operational-validation ZIP complete the inputs. Twenty-five
manifests describe about 1.59 GB of raw files, ignored under `data/raw/`.
The primary panel has 222 cells on twelve routes and 618,324 sampled passenger
weights. Coupon was not needed or downloaded. No synthetic research records.

Sixteen offline tests pass. Three full raw builds were run. After aligning both
entry paths to canonical round-trip float parsing, the final raw rebuild reproduced
all nineteen table/figure files byte-for-byte. See
[verification record](../../outputs/reports/verification.json).

## Reproduce

From repository root, with Python 3.13 and curl:

```powershell
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
.\.venv\Scripts\python.exe -m pytest -q
.\.venv\Scripts\python.exe -m src.acquisition.batch --kind all
.\.venv\Scripts\python.exe -m src.run
```

Use the environment's Python path on other platforms. See README for data terms,
cache behavior, memory/storage considerations, and a convenience analysis-only run.

## Git publication blocker

Public origin was verified and fetch worked. The final push used disabled terminal,
askpass, and credential-manager interaction; it failed with unavailable GitHub
username credentials (exit 128). No new login dialog was opened. All commits are
local; no force push, merge, history rewrite, or credential changes were performed.
After restoring Git authentication, publish with:

```text
git push -u origin codex/stage-0-to-4
```

Do not drop earlier local governance commits when publishing this branch.

## Local files and next actions

The user's pre-existing `.env.example` edit remains intentionally uncommitted.
Ignored `.env` contains the local key; do not print or commit it. Raw/intermediate
data, `.venv`, and a verification snapshot remain local and ignored.

No additional paid services were used. FR24 ledger: zero calls, zero credits;
6,000 initial-pass cap remains. Before Stage 5, establish demand identification or
obtain aligned quote/inventory/forecast data. Do not expand this weak aggregate
specification merely because the acquisition pipeline can scale.
