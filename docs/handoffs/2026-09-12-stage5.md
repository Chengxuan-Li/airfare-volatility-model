# Stage 5 handoff

Read AGENTS.md, README, docs/INDEX.md and docs/project_state.md first. Branch:
`codex/stage-5-robustness`, including prior Stage0-4 history.

## Completed and verified

Four additional DB1B Market/Ticket archives (2025Q1/Q2), three annual BTS T100
extracts and seven extended ERA5 files; all free. Market/Ticket were downloaded,
not Coupon. Raw archives remain ignored, with tracked exact provenance.

All 42 directions among the seven original airports produce905 primary and873
one-way product cells. Capacity/risk matches are complete. The 62 predeclared
specifications yield 54 fits and eight saturated holdout failures. Stronger
seasonal effects, independent route capacity/traffic, whole-ticket fares and
leave-one-airport checks preserve the weak descriptive assessment.

Main result: expanded training interaction+50.58[-38.02,139.19], holdout
+33.88[-112.42,180.18], using approximate pair-cluster intervals. These cannot
identify the original quote-time interaction. See the formal argument and report.
Stop criterion met by that identification argument, not by a causal-null claim,
favorable p-value, or supposed exhaustion of every public source/method.

Twenty-one offline tests pass; two complete raw builds reproduce 15 derived outputs
byte-for-byte. All 30 input identities, hashes and payloads validate. Original
Stage0-4 outputs remain unchanged. Independent code and report reviews completed;
all material findings addressed. Hashes: `outputs/stage5/verification.json`.
Reproduction/structure: `docs/methods/stage5_reproduction.md`.

## Commits and publication

- `e8c99f4`: extension pre-specification.
- `5201a91`: tested extension acquisition/pipeline and identification argument.
- `0738495`: reviewed expanded results, outputs and documentation.
- Subsequent final verification/handoff commit completes the pass.

Origin remains the configured public GitHub repository. Fetch succeeds, but
noninteractive push fails exit 128 because username credentials are unavailable.
No interactive login was opened. After restoring authentication, push
`codex/stage-5-robustness` normally; do not force-push or rewrite history.

## Preserved local state and future boundary

The pre-existing `.env.example` user edit is deliberately uncommitted. `.env` is
ignored and never printed/committed. Raw data, environment, and temporary hash
snapshot are ignored. Zero FR24 calls/credits; cap remains 6,000 against reported
60,000 monthly credits. No new paid services, prospective collection or automations.

Future mechanism research needs aligned quotes, product/query/departure times,
forecast vintages and an explicit identification design. Further aggregate
regressions alone cannot recover the absent joint information state.
