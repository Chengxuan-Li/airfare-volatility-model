# Local credentials; only the empty example is version controlled.
.env
.env.*
!.env.example

# Local Python environments and caches.
.venv/
venv/
__pycache__/
*.pyc
.pytest_cache/
.ipynb_checkpoints/

# Downloaded research inputs stay local; provenance manifests are tracked.
data/raw/
data/interim/
data/processed/

# Previous annual output sets retained for rollback/provenance, not publication.
outputs/stage6/.*.backup-*/
